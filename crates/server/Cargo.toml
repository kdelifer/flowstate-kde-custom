[package]
name = "flowstate-server"
version = "0.0.0"
edition = "2024"
publish = false
description = "Server Edge for Flowstate multiplayer"

[dependencies]
flowstate-sim = { path = "../sim" }
flowstate-wire = { path = "../wire" }
flowstate-replay = { path = "../replay" }
prost = "0.13"
rusty_enet = "0.4"

[dev-dependencies]
flowstate-client = { path = "../client" }

[lints.rust]
unsafe_code = "deny"
